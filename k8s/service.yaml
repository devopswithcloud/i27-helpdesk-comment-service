apiVersion: v1
kind: Service
metadata:
  name: i27-comment
  labels:
    app: i27-comment
spec:
  type: ClusterIP
  selector:
    app: i27-comment
  ports:
    - name: http
      port: 80
      targetPort: 8083
